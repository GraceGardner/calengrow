name: tests

on: [pull_request]

jobs:
  cypress-run:
    runs-on: ubuntu-latest
    steps:
      - name: checkout
        uses: actions/checkout@v2
      - run: CI=false
      - name: cypress run
        uses: cypress-io/github-action@v2
        with:
          build: npm run build
          start: npm start
